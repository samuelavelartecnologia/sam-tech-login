// Configuração do Firebase
const firebaseConfig = {
    apiKey: "sua-api-key",
    authDomain: "sam-tech-login.firebaseapp.com",
    projectId: "sam-tech-login",
    storageBucket: "sam-tech-login.appspot.com",
    messagingSenderId: "seu-sender-id",
    appId: "seu-app-id"
};

// Inicializa o Firebase
firebase.initializeApp(firebaseConfig);
